#include <iostream>
#include <string>
//#include <conio.h>
#include <algorithm>
//#include <cassert>
//#include<stack>
#include<queue>
#include<map>
#include <cmath>
#include<vector>
#define ll long long
using namespace std;

void fast()
{
    cin.sync_with_stdio(0);
    cin.tie(0);
}






int main()
{
    // fast();



    int s, m;
    cin >> s >> m;

    map<int, int> freq;

    for (size_t i = 0; i < s; i++) {
        int num;
        cin >> num;

        freq[num]++;
    }

    for (auto it = mp.begin(); it < m; it++) {
        cout << it.seconed<< endl;
    }










}
